package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Palette (Bright, Optimistic: Deep Blue + Orange Accent)
val PrimaryBlue = Color(0xFF1E40AF)      // Strong, accessible navy blue
val PrimaryBlueLight = Color(0xFF3B82F6) // Bright digital accent blue
val AccentOrange = Color(0xFFEA580C)     // Vibrant optimistic orange
val AccentOrangeLight = Color(0xFFF97316)// Secondary light orange
val SuccessGreen = Color(0xFF10B981)     // Energetic sync green

// Slate Backgrounds for visual depth
val BackgroundLight = Color(0xFFF1F5F9)  // Soft light slate background
val SurfaceCardLight = Color(0xFFFFFFFF) // High contrast pure white cards
val TextDeepSlate = Color(0xFF0F172A)    // High contrast readable body text
val TextMutedSlate = Color(0xFF64748B)   // Subtitle text

// Dark Theme Palette
val PrimaryBlueDark = Color(0xFF60A5FA)  // Accessible sky blue
val AccentOrangeDark = Color(0xFFFB923C) // Warm glowing orange
val BackgroundDark = Color(0xFF0F172A)   // Deep midnight slate background
val SurfaceCardDark = Color(0xFF1E293B)  // Dark card background
val TextLightSlate = Color(0xFFE2E8F0)   // High contrast white slate
val TextMutedDark = Color(0xFF94A3B8)    // Muted grey text
